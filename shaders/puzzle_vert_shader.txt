// puzzle_vert_shader.txt

attribute vec3 vertex;

uniform mat4 transform_matrix;
uniform mat4 permutation_transform_matrix;

void main()
{
    gl_Position = transform_matrix * permutation_transform_matrix * vec4(vertex, 1.0);
}